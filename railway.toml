# Valuation Studio — Railway Deployment Config
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn api.index:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "valuation-studio"
